name: GitHub Actions Labs

on: push

jobs:
  java-main:
    name: GitHub Actions Build
    runs-on: 'java'

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Test Check Out
        run: |
          ls
