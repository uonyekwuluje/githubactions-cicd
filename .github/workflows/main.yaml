name: GitHub Actions Labs

on: 
  push:
    branches:
      - master
  pull_request:
    branches:
      - master 

jobs:
  java-main:
    name: GitHub Actions Build
    runs-on: 'ubuntu-latest'

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Test Check Out
        run: |
          ls

      - name: Test Go Code One
        run: |
          go run godemo/hello.go

      - name: Test Go Code Two
        run: |
          go run godemo2/hello.go
